# Copie para .streamlit/secrets.toml (não versionado — já está no .gitignore)
# Streamlit Cloud / HF Space: cole o mesmo bloco no painel de Secrets.

[auth.users]
admin = "troque-esta-senha"
colaborador = "outra-senha"
