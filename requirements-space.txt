# HF Spaces / local com ESM-2 (PyTorch CPU)
# Inclui o stack Cloud + torch/transformers.
-r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
transformers==4.44.2
huggingface-hub==0.24.6
# Opcional (Qwen GGUF local): pip install llama-cpp-python==0.3.4
